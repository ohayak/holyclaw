{
    "$schema": "https://opencode.ai/config.json",
    "provider": {
        "openai": {
            "options": {
                "baseUrl": "${env:CLIPROXY_BASE_URL}",
                "apiKey": "${env:CLIPROXY_API_KEY}"
            }
        },
        "anthropic": {
            "options": {
                "baseUrl": "${env:CLIPROXY_BASE_URL}",
                "apiKey": "${env:CLIPROXY_API_KEY}"
            }
        }
    }
}